# 🎥 Руководство по видео-аналитике Sprint-Bot

**Дата:** 22 ноября 2025
**Статус:** 🚧 Пивот: оставляем только видеоаналитику

---

## Цель
Принять mp4-видео, в реальном времени определить оси тела пловца, сплиты, скорость и технику гребков, вернуть аннотированные кадры + отчёт.

## Основной поток

1. **Получение видео** — ваш локальный скрипт принимает путь к mp4 и сохраняет копию во временную директорию (без Telegram и API).
2. **Извлечение кадров** — `FrameExtractor` режет видео на кадры (настраиваемый FPS, окно до полной дистанции).
3. **Детекция пловца и позы** — `SwimmerDetector` (YOLO + pose estimation):
   - бокс пловца и дорожки;
   - ключевые точки: голова, плечо, таз, колено, стопа, кисть;
   - продольная/поперечная оси корпуса, контроль углов головы.
4. **Сплиты и скорость** — `SplitAnalyzer`:
   - реакция старта и касания поворотов;
   - вычисление сплитов и кривой скорости;
   - частота гребков и частота ударов ногами.
5. **Качество ударов и циклов** — подсчёт ударов на выходе и в цикле, прирост дистанции и скорости за удар/цикл.
6. **Траектория гребков** — трекинг кисти по фазам (вход, захват, протяжка) с подсказками по коррекции траектории.
7. **Отчёт и выдача** — `ReportGenerator` формирует:
   - аннотированные кадры с осями тела и траекторией рук;
   - график скорости;
   - таблицу сплитов (старт, повороты, финиш) и метрики ударов;
   - PDF/PNG с рекомендациями.
8. **Видеовывод** — `VideoOverlayGenerator` собирает mp4 с боксами, осями и отметками старта/поворотов, чтобы можно было смотреть ключевые моменты в реальном времени.

## Минимальный API для локальных скриптов (Python)

```python
from video_analysis import (
    extract_frames_from_video,
    detect_swimmer_in_frames,
    analyze_swimming_video,
    ReportGenerator,
)

frames = extract_frames_from_video("video.mp4", fps=2)
detections = detect_swimmer_in_frames(frames["frames"])
analysis = analyze_swimming_video(detections["detections"])
generator = ReportGenerator()
report = generator.generate_complete_report(analysis, "Атлет")
```

### Быстрый запуск без кода (CLI)

```bash
python examples/run_local_video_analysis.py \
  --video path/to/video.mp4 \
  --output ./run_outputs/session_01 \
  --fps 2 \
  --pool-length 25 \
  --athlete "Атлет"
```

Зависимости: `pip install -r requirements.txt` + `pip install -r video_analysis/requirements.txt` (+ `sudo apt-get install -y libgl1` для OpenCV в Linux).

## Готовые модули

- `video_analysis/frame_extractor.py` — извлечение кадров.
- `video_analysis/swimmer_detector.py` — детекция пловца и позы.
- `video_analysis/split_analyzer.py` — сплиты, скорость, частота.
- `video_analysis/report_generator.py` — текст, графики, PDF.

## Ближайшие шаги

- [ ] Добавить явное построение продольной/поперечной оси и контроль углов головы в выводе.
- [ ] Детектировать реакцию старта и повороты для сплитов по всей дистанции.
- [ ] Подсчёт ударов ногами на выходах и в циклах + метрики продвижения.
- [ ] Трекинг траектории кистей и рекомендации по корректировке.
- [ ] Обновить PDF-отчёт: аннотированные кадры + таблицы метрик.

## Требования к окружению

```bash
pip install -r requirements.txt
pip install -r video_analysis/requirements.txt
```

Запуск локального анализа: импортируйте функции в свой скрипт и передайте путь к mp4. Telegram и Docker не требуются.
