# README screenshots

The main `README.md` preview grid expects these three captures of the live
web app (`http://localhost:3000`). Save your screenshots here with **exactly**
these filenames (PNG):

| File | Page | What to capture |
|------|------|-----------------|
| `rehab-live.png` | `/rehabilitation` | Live rehabilitation studio — postural map, ROM / reps / asymmetry tiles, camera-angle panel |
| `ai-assistant.png` | `/assistant` | SPRINT AI Coach chat with suggested prompts and active context |
| `dryland.png` | `/dryland` | Dryland dashboard — metric cards + AI insights |

Tip: capture at a 16:10 window (e.g. ~1600px wide) for a crisp, consistent grid.
Once the files are committed, the preview renders automatically in the README.
